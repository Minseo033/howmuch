diff a/lib/features/onboarding/presentation/widgets/onboarding_page.dart b/lib/features/onboarding/presentation/widgets/onboarding_page.dart	(rejected hunks)
@@ -196,66 +197,90 @@ class _OnboardingSlideView extends StatelessWidget {
 
     return LayoutBuilder(
       builder: (context, constraints) {
-        final bottomGap = safeBottom > 0 ? safeBottom / 2 : 20.0;
-        final fixedHeight = 64 + 34 + 153 + 28 + 6 + 20 + 52 + 30 + bottomGap;
-        final artworkHeight = (constraints.maxHeight - fixedHeight).clamp(
-          240.0,
-          320.0,
-        );
-
-        return Padding(
-          padding: const EdgeInsets.symmetric(horizontal: 20),
-          child: Column(
-            crossAxisAlignment: CrossAxisAlignment.center,
-            children: [
-              const SizedBox(height: 64),
-              SizedBox(
-                height: artworkHeight,
-                child: Center(
-                  child: FittedBox(
-                    fit: BoxFit.contain,
-                    child: _ArtworkLayer(artwork: slide.artwork),
+        return SingleChildScrollView(
+          child: ConstrainedBox(
+            constraints: BoxConstraints(minHeight: constraints.maxHeight),
+            child: Padding(
+              padding: EdgeInsets.fromLTRB(24, 28, 24, safeBottom + 24),
+              child: Column(
+                crossAxisAlignment: CrossAxisAlignment.start,
+                children: [
+                  Row(
+                    mainAxisAlignment: MainAxisAlignment.spaceBetween,
+                    children: [
+                      const NeighborhoodWordmark(compact: true),
+                      Text(
+                        '0${step + 1} / 0$totalSteps',
+                        style: const TextStyle(
+                          color: OnboardingPage.muted,
+                          fontSize: 11,
+                          letterSpacing: 2,
+                        ),
+                      ),
+                    ],
                   ),
-                ),
-              ),
-              const SizedBox(height: 34),
-              SizedBox(height: 153, child: _SlideCopy(slide: slide)),
-              const SizedBox(height: 28),
-              _StepIndicator(step: step, totalSteps: totalSteps),
-              const SizedBox(height: 20),
-              SizedBox(
-                width: double.infinity,
-                height: 52,
-                child: _PrimaryButton(
-                  label: slide.primaryLabel,
-                  onPressed: onNext,
-                ),
-              ),
-              if (isLast && onSkip != null) ...[
-                const SizedBox(height: 12),
-                SizedBox(
-                  height: 18,
-                  child: TextButton(
-                    onPressed: onSkip,
-                    style: TextButton.styleFrom(
-                      padding: EdgeInsets.zero,
-                      foregroundColor: OnboardingPage.muted,
-                      textStyle: const TextStyle(
-                        fontFamily: OnboardingPage.fontFamily,
-                        fontFamilyFallback: OnboardingPage.fontFallback,
-                        fontSize: 12,
-                        fontWeight: FontWeight.w500,
-                        height: 1.5,
+                  const SizedBox(height: 24),
+                  Container(
+                    height: (constraints.maxHeight * .36).clamp(210.0, 310.0),
+                    width: double.infinity,
+                    decoration: BoxDecoration(
+                      color: const Color(0xFFE4EDCF),
+                      borderRadius: BorderRadius.circular(32),
+                      border: Border.all(color: const Color(0xFFD4DFC2)),
+                    ),
+                    child: ClipRRect(
+                      borderRadius: BorderRadius.circular(32),
+                      child: Center(
+                        child: FittedBox(
+                          fit: BoxFit.contain,
+                          child: _ArtworkLayer(artwork: slide.artwork),
+                        ),
                       ),
                     ),
-                    child: const Text('로그인 없이 둘러보기'),
                   ),
-                ),
-              ] else ...[
-                const SizedBox(height: 30),
-              ],
-              SizedBox(height: bottomGap),
-            ],
+                  const SizedBox(height: 24),
+                  _SlideCopy(slide: slide),
+                  const SizedBox(height: 24),
+                  Row(
+                    children: [
+                      _StepIndicator(step: step, totalSteps: totalSteps),
+                      const Spacer(),
+                      const Text(
+                        '좋은 가격, 가까운 일상.',
+                        style: TextStyle(
+                          color: OnboardingPage.muted,
+                          fontSize: 10,
+                        ),
+                      ),
+                    ],
+                  ),
+                  const SizedBox(height: 18),
+                  SizedBox(
+                    width: double.infinity,
+                    height: 52,
+                    child: _PrimaryButton(
+                      label: slide.primaryLabel,
+                      onPressed: onNext,
+                    ),
+                  ),
+                  if (isLast && onSkip != null) ...[
+                    const SizedBox(height: 12),
+                    Center(
+                      child: TextButton(
+                        onPressed: onSkip,
+                        child: const Text(
+                          '로그인 없이 둘러보기',
+                          style: TextStyle(
+                            color: OnboardingPage.muted,
+                            fontSize: 12,
+                          ),
+                        ),
+                      ),
+                    ),
+                  ],
+                ],
+              ),
+            ),
           ),
         );
       },
